fastapi>=0.100.0
uvicorn[standard]>=0.20.0
supabase>=1.0.0,<2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.28.0
urllib3>=1.26.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
httpx>=0.24.0

# Playwright — only needed if Dental Elite is JS-rendered.
# Uncomment + add "playwright install chromium" to Procfile build command.
# playwright==1.44.0
